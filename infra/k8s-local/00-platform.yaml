apiVersion: v1
kind: Pod
metadata:
  name: platform-apicurio
  labels:
    app: apicurio-registry
spec:
  containers:
    - name: apicurio-registry
      image: quay.io/apicurio/apicurio-registry:3.3.0
      ports:
        - containerPort: 8080
          hostPort: 8081
